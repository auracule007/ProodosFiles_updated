from django.urls import path
from . import views

# 
urlpatterns = [
    # path('<uuid:folder_id>/', views.view_folder, name='view_folder'),
    # path('share/<uuid:folder_id>/', views.share_folder, name='share_folder'),
    # path('star/<uuid:folder_id>/', views.star, name='star_folder'),
    # path('bin/<uuid:folder_id>/', views.bin, name='bin_folder'),
    # path('delete-folder/<uuid:folder_id>/', views.delete_permanently, name='delete_folder'),
    # path('copy-shared/<uuid:folder_id>/', views.copy_shared_folder, name="copy_shared_folder"),
]